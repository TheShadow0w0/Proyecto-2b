using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class a : MonoBehaviour
{
    float vida = 0;
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Proyecto troll");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
